{
	"version": 2,
	"builds": [
		{
			"src": "server/index.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "/server/index.js"
		}
	],
	"buildCommand": "npm install --prefix server && npm install --prefix app && npm run build --prefix app"
}
